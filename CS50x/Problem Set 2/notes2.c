#include <stdio.h>


int main(void)
{
	int arraycount[100];
		for (int i = 0, i <100, i++)
		{
			arraycount[i] = i; 
		}

}

int addprint(int x)
{
	int sum = 0
	int adder = x
	int sum = adder + sum
}